# Product source

`CStructSharp/` is the .NET 8/.NET 10 library. `CStructSharp.Wasm/` is the managed WebAssembly bridge (exports, DTOs, JSON projection); the JavaScript adapter, declarations, and bundle pieces live in `packages/cstructsharp/`.
