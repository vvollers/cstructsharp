# Automation

`workflows/` builds and tests managed code, both independent browser apps, npm consumers, and documentation. Release and website publishing are explicit manual workflows; pushing source does not publish packages or deploy the website. `dependabot.yml` covers managed, npm, and action dependencies. The pull request template is optional contributor guidance. See [release instructions](../docs/project/release-process.md).
