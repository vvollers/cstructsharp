# Local .NET tools

`dotnet-tools.json` pins DocFX, Stryker, the public API generator. Run `dotnet tool restore` from the repository root. Tools support documentation and quality checks; their caches and reports are generated, ignored output.
