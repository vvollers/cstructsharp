# npm package inputs

`cstructsharp/` contains authored Node/browser loaders, the Vite integration, copy command, package metadata, and README. It is a private staging template, not a complete installable package. Run `npm run pack:npm` then `npm run test:npm` from `apps/explorer/` after publishing WASM. The actual tarball, runtime, types, and notices are assembled under ignored `artifacts/npm/`.
